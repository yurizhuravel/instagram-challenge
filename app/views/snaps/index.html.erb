<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Snapogram</title>
  </head>
  <body>
    <h1>Snapogram</h1>

    <h3><a href='<%= new_snap_path %>'>Add a snap</a><h3>

    <br>

  <% if Snap.all.any? %>
    <% Snap.all.reverse.each do |snap| %>
      <a href='<%= snap.image.url %>'><%= image_tag snap.image.url(:medium) %></a>
      <h3><%= snap.caption %></h3>
        <% snap.comments.each do |comment| %>
          <p><%= comment.thoughts %></p>
        <% end %>
      <p><%= link_to "Delete snap", snap_path(snap), method: :delete %></p>
      <p><%= link_to "Add a comment", new_snap_comment_path(snap) %></p>
    <% end %>
  <% else %>
    <h1>No snaps yet</h1>
    <% end %>

  </body>
</html>
